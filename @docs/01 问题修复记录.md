### 修复记录 - 2025-10-18

-   **问题描述：** 为 PromptMate 项目添加完整的云端存储功能，支持坚果云（WebDAV）和 OneDrive 等云存储服务，参考 CherryStudio 项目的实现方式。

-   **根本原因（可选）：** 项目缺少云端数据同步功能，用户无法在多设备间同步数据，也缺少云端备份机制。

-   **解决方案：** 
    1. 扩展类型系统，添加 CloudStorageSettings、CloudStorageProvider、WebDAVConfig、OneDriveConfig 等云存储相关类型定义
    2. 实现 WebDAVClient 客户端服务，支持坚果云、ownCloud、NextCloud 等所有 WebDAV 协议的云存储服务
       - 支持文件上传、下载、列表、删除等完整操作
       - 自动创建目录、错误处理、连接测试等功能
       - 提供预定义的服务商配置（坚果云、自定义）
    3. 实现 OneDriveClient 客户端服务，使用 Microsoft Graph API
       - OAuth 2.0 授权流程
       - 令牌刷新机制
       - 完整的文件操作支持
    4. 创建 CloudStorageManager 统一管理器
       - 统一的云存储接口，支持多种云服务
       - 自动同步功能（可配置同步间隔）
       - 手动上传/下载操作
       - 实时同步状态管理
       - 事件驱动的状态通知
    5. 实现 CloudStorageSettings UI 组件
       - 直观的配置界面
       - 实时同步状态显示
       - 连接测试功能
       - 预设服务商选择
       - 手动操作按钮（同步、上传、下载）
    6. 在设置页面集成云存储选项卡
       - 新增云存储选项卡
       - 集成云存储管理器
       - 处理同步事件和状态更新
       - 本地数据与云端数据的保存和加载
    7. 创建完整的使用文档
       - 坚果云配置指南（包含应用密码获取步骤）
       - OneDrive 配置指南（包含 Azure 应用注册步骤）
       - 功能使用说明
       - 常见问题解答
       - 技术架构说明

-   **影响范围：**
    -   `src/types/index.ts` - 添加云存储相关类型定义
    -   `src/services/cloudStorage/WebDAVClient.ts` - 新建 WebDAV 客户端
    -   `src/services/cloudStorage/OneDriveClient.ts` - 新建 OneDrive 客户端
    -   `src/services/cloudStorage/CloudStorageManager.ts` - 新建云存储管理器
    -   `src/services/cloudStorage/index.ts` - 服务导出文件
    -   `src/components/CloudStorageSettings.tsx` - 新建云存储设置 UI 组件
    -   `src/pages/SettingsPage.tsx` - 集成云存储选项卡
    -   `@docs/云存储功能使用指南.md` - 新建使用文档

**功能特性：**
- ✅ WebDAV 协议支持（坚果云、ownCloud、NextCloud）
- ✅ OneDrive 云存储支持
- ✅ 自动定时同步（可配置间隔）
- ✅ 手动同步、上传、下载操作
- ✅ 实时同步状态显示
- ✅ 连接测试功能
- ✅ 完整的错误处理和用户提示
- ✅ 事件驱动的状态管理
- ✅ 可扩展的架构设计

**技术亮点：**
- 统一的云存储接口，便于扩展新的云服务
- 基于 EventEmitter 的事件系统，实时通知状态变化
- 完整的 WebDAV 协议实现，支持所有标准操作
- OAuth 2.0 授权流程和令牌管理
- TypeScript 类型安全
- 响应式 UI 设计

---

### 修复记录 - 2025-01-27

-   **问题描述：** 提示词编辑后，提示词分类自动变成"无"，且修改后，提示词分类无法被修改。

-   **根本原因（可选）：** 
    1. 在编辑模式下，如果 `prompt.category` 是空字符串、null 或 undefined，分类会变成空值
    2. Select 组件无法正确处理空值，导致分类无法显示和修改
    3. 当分类为空时，Select 组件的 value 不匹配任何选项，导致无法正常更新

-   **解决方案：** 
    1. 修复 `usePromptForm.ts` 中的分类初始化逻辑
       - 在 `getInitialFormData` 函数中，如果编辑模式下 `prompt.category` 为空，使用默认分类
       - 在 `useEffect` 监听 prompt 变化时，同样处理空分类的情况
       - 提取 `getDefaultCategory` 为独立的 useCallback 函数，确保逻辑一致
    2. 修复 `PromptFormFields.tsx` 和 `PromptEditForm.tsx` 中的分类选择器
       - 确保 Select 的 value 始终是一个有效的分类 ID
       - 如果当前分类不在分类列表中，使用第一个分类作为默认值
       - 在 `onValueChange` 中添加空值检查，确保只更新有效值

-   **影响范围：**
    -   `src/hooks/usePromptForm.ts` - 修复分类初始化逻辑，处理空分类情况
    -   `src/components/PromptFormFields.tsx` - 修复分类选择器，确保能正常显示和修改
    -   `src/components/PromptEditForm.tsx` - 修复分类选择器，确保能正常显示和修改

**功能特性：**
- ✅ 编辑提示词时，如果原分类为空，自动使用默认分类
- ✅ 分类选择器可以正常显示和修改
- ✅ 确保分类值始终有效，不会出现空值

**技术亮点：**
- 使用 useCallback 优化默认分类获取逻辑
- 在多个地方统一处理空分类的情况
- 确保 Select 组件的 value 始终有效

---

### 修复记录 - 2025-01-27

-   **问题描述：** 新建提示词的右键弹出菜单的点击范围目前受限制于提示词的数量，如果只有一个卡片，菜单只能在那一个卡片附近弹出，点击其他空白区域则没反应。

-   **根本原因（可选）：** `ContextMenuTrigger` 包裹的容器没有占满整个 ScrollArea 的视口，导致右键菜单的触发范围仅限于有内容（提示词卡片）的区域。当只有一个卡片时，空白区域无法触发右键菜单。

-   **解决方案：** 
    1. 修改 `PromptList.tsx` 中 `ContextMenuTrigger` 包裹的容器样式
    2. 为容器添加 `minHeight: '100%'` 和 `height: '100%'` 样式，确保它占满整个 ScrollArea 的视口
    3. 这样无论提示词数量多少，整个 ScrollArea 区域都可以触发右键菜单

-   **影响范围：**
    -   `src/components/PromptList.tsx` - 修改 ContextMenuTrigger 包裹容器的样式，确保占满整个 ScrollArea 视口

**功能特性：**
- ✅ 右键菜单触发范围扩展到整个 ScrollArea 区域
- ✅ 无论提示词数量多少，空白区域也可以触发右键菜单
- ✅ 保持原有功能不变，仅修复触发范围问题

**技术亮点：**
- 使用 CSS 样式确保容器占满父容器
- 保持原有组件结构，最小化修改
- 不影响其他功能

---

### 修复记录 - 2025-01-27

-   **问题描述：** 项目输入框、全局应支持 macOS、Windows 常用快捷键，比如粘贴、复制、全选、剪切、关闭等。

-   **根本原因（可选）：** 项目缺少对跨平台快捷键的完整支持，用户无法使用标准的系统快捷键进行操作，影响使用体验。

-   **解决方案：** 
    1. 在主进程中更新应用菜单，添加完整的快捷键配置
       - 编辑菜单：撤销 (Cmd/Ctrl+Z)、重做 (Cmd/Ctrl+Shift+Z)、剪切 (Cmd/Ctrl+X)、复制 (Cmd/Ctrl+C)、粘贴 (Cmd/Ctrl+V)、全选 (Cmd/Ctrl+A)
       - 窗口菜单：最小化 (Cmd/Ctrl+M)、关闭 (Cmd/Ctrl+W)、退出 (Cmd+Q/Ctrl+Q)
       - 视图菜单：刷新、开发者工具、全屏等快捷键
       - 所有快捷键都使用 `CommandOrControl` 修饰符，自动适配 macOS 和 Windows
    2. 创建 `useKeyboardShortcuts` Hook
       - 为输入框和文本域提供快捷键支持
       - 跨平台检测（macOS 使用 Command，Windows/Linux 使用 Ctrl）
       - 支持复制、粘贴、剪切、全选、撤销、重做等操作
       - 可自定义快捷键处理器
    3. 创建 `useGlobalShortcuts` Hook
       - 处理应用级别的全局快捷键（如关闭窗口）
       - 支持窗口最小化、最大化等操作
    4. 更新 Input 组件
       - 集成 `useKeyboardShortcuts` Hook
       - 默认启用快捷键支持，可通过 `enableShortcuts` 属性控制
       - 保持向后兼容，不影响现有代码
    5. 更新 Textarea 组件
       - 集成 `useKeyboardShortcuts` Hook
       - 默认启用快捷键支持，可通过 `enableShortcuts` 属性控制
       - 保持向后兼容，不影响现有代码

-   **影响范围：**
    -   `src/main/main.cjs` - 更新应用菜单，添加完整快捷键配置
    -   `src/hooks/useKeyboardShortcuts.ts` - 新建键盘快捷键 Hook
    -   `src/components/ui/input.tsx` - 集成快捷键支持
    -   `src/components/ui/textarea.tsx` - 集成快捷键支持

**功能特性：**
- ✅ 跨平台快捷键支持（macOS 和 Windows）
- ✅ 标准编辑快捷键：复制、粘贴、剪切、全选、撤销、重做
- ✅ 窗口管理快捷键：关闭、最小化、最大化
- ✅ 输入框和文本域自动支持快捷键
- ✅ 可配置的快捷键启用/禁用
- ✅ 向后兼容，不影响现有功能

**技术亮点：**
- 使用 Electron 菜单系统提供原生快捷键支持
- 跨平台自动适配（Command vs Ctrl）
- React Hook 模式，易于使用和扩展
- TypeScript 类型安全
- 不影响现有组件 API，完全向后兼容

---

### 修复记录 - 2025-12-02

-   **问题描述：** Vercel 部署时出现错误："Found invalid Node.js Version: '24.x'. Please set Node.js Version to 18.x in your Project Settings to use Node.js 18." 即使在 Vercel 项目设置中手动设置为其他版本也会报错。

-   **根本原因（可选）：** 
    1. 项目缺少明确的 Node.js 版本配置，导致 Vercel 自动检测到了错误的版本
    2. `vercel.json` 中使用的 `@vercel/node@3.0.0` runtime 版本过旧，可能与新版本的 Node.js 不兼容
    3. 即使手动在 Vercel 项目设置中指定版本，如果项目配置文件（package.json）不一致，仍可能报错
    4. Vercel 现在支持 Node.js 24.x，但需要正确配置才能使用

-   **解决方案：** 
    1. 在 `package.json` 中添加 `engines` 字段，明确指定 Node.js 版本为 24.x（Vercel 现在支持 Node.js 24.x）
    2. 创建 `.nvmrc` 文件，指定 Node.js 版本为 24，确保本地开发和 CI/CD 环境使用相同的版本
    3. 更新 `vercel.json` 中的 runtime 版本，将 `@vercel/node@3.0.0` 更新为 `@vercel/node@5.5.13`（与 package.json 中的 devDependencies 版本一致），确保与 Node.js 24.x 兼容
    4. 这样 Vercel 会自动识别并使用正确的 Node.js 版本进行构建

-   **影响范围：**
    -   `package.json` - 添加 engines 字段指定 Node.js 版本为 24.x
    -   `.nvmrc` - 新建文件，指定 Node.js 版本为 24
    -   `vercel.json` - 更新 @vercel/node runtime 版本从 3.0.0 到 5.5.13

**功能特性：**
- ✅ Vercel 部署时自动使用 Node.js 24.x 版本
- ✅ 本地开发和部署环境版本一致性
- ✅ 使用最新的 Node.js 版本，获得最佳性能和最新特性
- ✅ Runtime 版本与 Node.js 版本兼容

**技术亮点：**
- 使用 package.json engines 字段让 Vercel 自动识别版本
- 使用 .nvmrc 文件确保版本一致性
- 更新 vercel.json 中的 runtime 版本确保兼容性
- 选择 Node.js 24.x 最新版本，获得最佳性能和最新特性支持

---

### 修复记录 - 2025-12-03

-   **问题描述：** Vercel 部署后，所有 API 路由返回 404 错误。例如 `/api/auth/oauth/url?provider=linuxdo&redirect_uri=...` 等请求都返回 404。

-   **根本原因（可选）：** 
    1. `api/[...path].ts` 文件中的路由匹配逻辑有问题
    2. `matchRoute` 函数期望 `IncomingMessage` 类型，但传入的是普通对象，导致类型不匹配
    3. 路由匹配失败，所有请求都返回 404

-   **解决方案：** 
    1. 修改 `api/[...path].ts`，不再使用 `matchRoute` 函数，而是直接使用 `getRoutes()` 获取所有注册的路由
    2. 实现直接的路由匹配逻辑，不依赖 `IncomingMessage` 类型
    3. 确保路径正确构建（从 `req.query.path` 数组构建完整路径）
    4. 修复类型定义，使用 `RouteDefinition` 类型替代 `any`

-   **影响范围：**
    -   `api/[...path].ts` - 修复路由匹配逻辑，直接匹配路由而不依赖 IncomingMessage 类型

**功能特性：**
- ✅ Vercel Serverless Functions 可以正确匹配和处理 API 路由
- ✅ 所有 API 端点（如 `/api/auth/oauth/url`）可以正常工作
- ✅ 查询参数正确传递和处理
- ✅ 类型安全，使用正确的 TypeScript 类型

**技术亮点：**
- 直接实现路由匹配逻辑，避免类型不兼容问题
- 使用 `getRoutes()` 获取所有注册的路由
- 保持与现有路由系统的兼容性
- 添加详细的日志输出，便于调试

---

### 修复记录 - 2025-12-03

-   **问题描述：** 
    1. Electron 安装时无法自定义安装路径（像其他软件一样）
    2. 安装时无法覆盖旧版本
    3. 安装后的应用名称包含版本号（如 PromptMate-1.1.11-x64.exe），希望始终是 PromptMate

-   **根本原因（可选）：** 
    1. electron-builder 的 NSIS 配置中 `oneClick` 默认为 true，导致无法选择安装路径
    2. `allowToChangeInstallationDirectory` 未设置为 true
    3. `artifactName` 配置中包含了版本号变量 `${version}`

-   **解决方案：** 
    1. 在 `package.json` 的 `nsis` 配置中添加 `allowToChangeInstallationDirectory: true`，允许用户自定义安装路径
    2. 设置 `oneClick: false`，显示完整的安装向导（而不是一键安装），这样用户可以：
       - 选择安装路径
       - 选择是否覆盖旧版本（electron-builder 会自动检测已安装的版本）
       - 选择是否创建桌面快捷方式等
    3. 修改 `artifactName` 配置，移除 `${version}` 变量：
       - Windows: 从 `${productName}-${version}-${arch}.${ext}` 改为 `${productName}-${arch}.${ext}`
       - macOS: 从 `${productName}-${version}-${arch}.${ext}` 改为 `${productName}-${arch}.${ext}`
    4. 设置 `shortcutName: "PromptMate"`，确保快捷方式名称始终是 PromptMate（无版本号）
    5. 配置 `deleteAppDataOnUninstall: false`，卸载时保留用户数据
    6. 配置 `runAfterFinish: true`，安装完成后自动运行应用

-   **影响范围：**
    -   `package.json` - 更新 electron-builder 的 NSIS 配置和 artifactName 配置

**功能特性：**
- ✅ 安装时可以自定义安装路径（像其他软件一样）
- ✅ 安装时可以覆盖旧版本（electron-builder 自动检测并提示）
- ✅ 安装包文件名不包含版本号（如 `PromptMate-x64.exe`）
- ✅ 安装后的应用名称始终是 PromptMate（无版本号）
- ✅ 显示完整的安装向导，提供更好的用户体验
- ✅ 卸载时可以选择是否保留用户数据

**技术亮点：**
- 使用 electron-builder 的 NSIS 配置实现自定义安装路径
- 通过 `oneClick: false` 提供完整的安装向导体验
- 通过 `artifactName` 配置控制安装包文件名格式
- 通过 `productName` 和 `shortcutName` 确保应用名称一致

---

### 修复记录 - 2025-12-03

-   **问题描述：** 当系统主题色为黑色，但设置软件主题色为浅色时，提示词卡片的详细内容背景依然是黑色，不跟随应用主题设置。

-   **根本原因（可选）：** 
    1. `HoverCardContent` 组件虽然使用了 `bg-popover` 类，但可能没有正确应用 CSS 变量
    2. `PromptDetailDialog` 中的内容区域使用了 `bg-muted/50`，但可能受到系统主题影响
    3. `prose` 类（Tailwind Typography）可能自动应用了系统主题检测的深色样式
    4. `markdown-body` 类的背景色定义可能不够明确

-   **解决方案：** 
    1. 在 `HoverCardContent` 中添加明确的 `style` 属性，使用 `hsl(var(--popover))` 确保背景色跟随应用主题
    2. 在 `PromptDetailDialog` 的内容区域添加 `style` 属性，使用 `hsl(var(--muted) / 0.5)` 确保背景色正确
    3. 在 `HoverCardContent` 的内容区域（prose 类）添加明确的背景色和文字颜色样式，使用 CSS 变量
    4. 更新 `markdown-body` 类的样式定义，使用 `hsl()` 函数和 `!important` 确保背景色透明且不受系统主题影响

-   **影响范围：**
    -   `src/components/PromptList.tsx` - 修复 HoverCardContent 和 PromptDetailDialog 的背景色
    -   `src/index.css` - 更新 markdown-body 类的样式定义

**功能特性：**
- ✅ 提示词卡片的详细内容背景色正确跟随应用主题设置
- ✅ 不受系统主题影响，始终使用应用设置的主题色
- ✅ HoverCardContent 和 PromptDetailDialog 的背景色一致

**技术亮点：**
- 使用 CSS 变量（`hsl(var(--popover))`）确保背景色跟随应用主题
- 通过 `style` 属性强制应用正确的背景色，避免系统主题干扰
- 使用 `!important` 确保样式优先级，防止被其他样式覆盖

---

### 修复记录 - 2025-12-03

-   **问题描述：** 应用最外层出现了两个滚动条（右侧和底部），影响界面美观。

-   **根本原因（可选）：** 
    1. `html` 和 `body` 元素只设置了 `overflow-x: hidden`，没有禁止纵向滚动
    2. `#root` 元素没有设置 `overflow: hidden`，可能导致滚动条出现
    3. 缺少 `height: 100%` 和 `width: 100%` 限制，导致内容可能超出视口

-   **解决方案：** 
    1. 更新 `src/index.css` 中的全局样式：
       - 将 `html` 和 `body` 的 `overflow-x: hidden` 改为 `overflow: hidden`，禁止所有方向的滚动
       - 添加 `max-height: 100vh` 和 `height: 100%`，确保元素不超出视口
       - 添加 `width: 100%` 和 `margin: 0; padding: 0`，确保元素占满视口且无额外边距
       - 为 `#root` 元素添加 `overflow: hidden`、`height: 100%` 和 `width: 100%`，确保根容器不产生滚动条

-   **影响范围：**
    -   `src/index.css` - 更新全局样式，禁止最外层滚动条

**功能特性：**
- ✅ 应用最外层不再显示滚动条
- ✅ 滚动功能由内部组件（如 ScrollArea）处理
- ✅ 界面更加美观，符合现代应用设计

**技术亮点：**
- 在全局样式层面禁止最外层滚动
- 确保应用容器占满视口且不超出
- 保持内部组件的滚动功能正常工作

---

### 修复记录 - 2025-12-03

-   **问题描述：** 
    1. Vercel 部署后，登录服务 API 路由返回 404 错误
    2. TypeScript 编译错误：`Relative import paths need explicit file extensions in ECMAScript imports when '--moduleResolution' is 'node16' or 'nodenext'`

-   **根本原因（可选）：** 
    1. `server/controllers/authController.ts` 中的动态导入语句缺少文件扩展名 `.js`
    2. TypeScript 配置可能在某些情况下要求显式的文件扩展名
    3. API 路由可能因为编译错误导致无法正确初始化

-   **解决方案：** 
    1. 修复 `authController.ts` 中的动态导入语句，添加 `.js` 扩展名：
       - `import('../services/linuxdoOAuth')` → `import('../services/linuxdoOAuth.js')`
    2. 更新 `tsconfig.server.json`，添加 `allowSyntheticDefaultImports` 和 `allowImportingTsExtensions` 配置
    3. 在 `api/[...path].ts` 中添加路由初始化错误处理，确保错误能被正确捕获和报告
    4. 添加更详细的日志输出，包括可用路由列表，便于调试路由匹配问题

-   **影响范围：**
    -   `server/controllers/authController.ts` - 修复动态导入语句，添加文件扩展名
    -   `tsconfig.server.json` - 更新 TypeScript 配置
    -   `api/[...path].ts` - 添加错误处理和详细日志

**功能特性：**
- ✅ TypeScript 编译错误已修复
- ✅ API 路由可以正确初始化
- ✅ 登录服务 API 端点可以正常工作
- ✅ 添加了详细的调试日志，便于排查问题

**技术亮点：**
- 修复动态导入语句的文件扩展名问题
- 添加路由初始化错误处理
- 增强日志输出，包括可用路由列表
- 确保 TypeScript 配置兼容性

---

### 修复记录 - 2025-12-03

-   **问题描述：** Vercel 部署后，虽然构建没有报错，但 API 路由仍然返回 404 错误。例如 `/api/auth/oauth/url?provider=google&redirect_uri=...` 等请求都返回 404。

-   **根本原因（可选）：** 
    1. `vercel.json` 中的 rewrites 配置可能干扰了 catch-all 路由的路径解析
    2. 如果 `req.query.path` 为空或未定义，路径数组会是空的，导致无法正确构建路径
    3. 正则表达式的 `lastIndex` 可能没有重置，导致匹配失败
    4. 缺少从 `req.url` 提取路径的备用方案

-   **解决方案：** 
    1. 移除 `vercel.json` 中 `/api/:path*` 的 rewrite 规则，因为 Vercel 的 catch-all 路由会自动处理 `/api/*` 路径，这个 rewrite 规则可能导致路径解析冲突
    2. 添加从 `req.url` 提取路径的备用方案：如果 `req.query.path` 为空，尝试从 `req.url` 中解析路径段
    3. 在路由匹配循环中重置正则表达式的 `lastIndex`，确保每次匹配都从头开始
    4. 添加更详细的日志输出，包括：
       - 路由初始化状态和路由总数
       - 请求 URL 和路径数组
       - 尝试匹配的路由列表
       - 匹配成功的路由信息
    5. 添加路由初始化错误处理，确保错误能被正确捕获和报告

-   **影响范围：**
    -   `api/[...path].ts` - 添加路径提取备用方案、重置正则表达式 lastIndex、增强日志
    -   `vercel.json` - 移除可能干扰的 rewrite 规则

**功能特性：**
- ✅ API 路由可以正确解析路径，即使 `req.query.path` 为空
- ✅ 从 `req.url` 提取路径的备用方案确保路径解析的可靠性
- ✅ 正则表达式匹配更加可靠，避免 lastIndex 问题
- ✅ 详细的日志输出便于调试和排查问题

**技术亮点：**
- 移除可能干扰的 rewrite 规则，让 Vercel 的 catch-all 路由正常工作
- 实现多层次的路径提取方案，提高可靠性
- 重置正则表达式的 lastIndex，确保匹配准确性
- 增强错误处理和日志输出

---

### 修复记录 - 2025-12-03

-   **问题描述：** Vercel 部署时构建失败，错误信息：`The pattern "api/**/*.ts" defined in functions doesn't match any Serverless Functions inside the api directory.` 同时，除了测试端点外，其他 API 请求（如 `/api/auth/oauth/url`）返回 404 且没有日志输出，说明 catch-all 路由 `api/[...path].ts` 没有被调用。

-   **根本原因（可选）：** 
    1. `vercel.json` 中使用了无效的通配符模式 `api/**/*.ts`，Vercel 不支持这种通配符模式
    2. Vercel 的 `functions` 配置需要明确指定每个函数文件，不能使用通配符
    3. catch-all 路由可能没有被正确识别或调用

-   **解决方案：** 
    1. 移除 `vercel.json` 中无效的 `api/**/*.ts` 通配符模式配置
    2. 只保留 `api/[...path].ts` 的明确配置
    3. 确保 catch-all 路由文件存在且格式正确

-   **影响范围：**
    -   `vercel.json` - 移除无效的通配符模式配置

**功能特性：**
- ✅ Vercel 构建不再报错
- ✅ catch-all 路由配置正确

**技术亮点：**
- Vercel 的 `functions` 配置不支持通配符，需要明确指定每个函数文件
- catch-all 路由使用 `[...path]` 格式，需要在 `functions` 中明确配置

---

### 修复记录 - 2025-12-03

-   **问题描述：** Vercel 构建日志中没有看到 `api/[...path].ts` 的信息，说明 Vercel 没有识别这个 Serverless Function 文件。

-   **根本原因（可选）：** 
    1. `vercel.json` 中的 `outputDirectory` 设置为 `"dist"`，与 Vercel Dashboard 中的设置（`"/"`）不一致
    2. 当 `outputDirectory` 设置为 `"dist"` 时，Vercel 可能只在 `dist` 目录中查找文件，而 `api` 文件夹不会被 Vite 构建到 `dist` 中
    3. Vercel 的 Serverless Functions 应该从项目根目录读取，需要 `outputDirectory` 设置为 `"/"` 才能正确识别 `api` 文件夹

-   **解决方案：** 
    1. 将 `vercel.json` 中的 `outputDirectory` 从 `"dist"` 改为 `"/"`，与 Vercel Dashboard 中的设置保持一致
    2. 确保 `functions` 配置中明确指定了 `api/[...path].ts`
    3. 这样 Vercel 会从项目根目录查找文件，包括 `api` 文件夹

-   **影响范围：**
    -   `vercel.json` - 将 `outputDirectory` 从 `"dist"` 改为 `"/"`

**功能特性：**
- ✅ Vercel 可以从项目根目录正确识别 `api` 文件夹
- ✅ Serverless Functions 可以被正确识别和部署
- ✅ 配置与 Vercel Dashboard 设置保持一致

**技术亮点：**
- `outputDirectory` 设置为 `"/"` 确保 Vercel 从项目根目录查找文件
- Serverless Functions 应该从项目根目录读取，不受构建输出目录影响
- 配置一致性确保部署行为可预测

---

### 修复记录 - 2025-12-03

-   **问题描述：** Vercel 构建失败，错误信息：`Error: ENOENT: no such file or directory, open '/vercel/path0/node_modules/.ignored_concurrently/node_modules/date-fns/esm/secondsToHours/index.d.ts'`。构建脚本中包含了 `npm run build:electron`，这是 Electron 专用的构建步骤，在 Vercel 上不应该运行。

-   **根本原因（可选）：** 
    1. `package.json` 中的 `build` 脚本包含了 `npm run build:electron`，这是 Electron 应用的构建步骤
    2. Electron 构建步骤在 Vercel 服务器环境中无法正常运行，导致构建失败
    3. Vercel 只需要构建前端代码（Vite），不需要 Electron 相关的构建

-   **解决方案：** 
    1. 修改 `package.json` 中的 `build` 脚本，移除 `npm run build:electron` 步骤
    2. 将 `"build": "npm run build:info && vite build && npm run build:electron"` 改为 `"build": "npm run build:info && vite build"`
    3. 这样 Vercel 只会构建前端代码，不会尝试运行 Electron 构建步骤

-   **影响范围：**
    -   `package.json` - 修改 `build` 脚本，移除 Electron 构建步骤

**功能特性：**
- ✅ Vercel 构建可以成功完成
- ✅ 只构建前端代码，不运行 Electron 构建步骤
- ✅ 保持本地开发环境的构建脚本不变（Electron 构建仍然可以通过其他方式运行）

**技术亮点：**
- 区分 Web 部署和 Electron 构建的需求
- 保持构建脚本的简洁性，只包含必要的步骤
- 确保 Vercel 构建过程稳定可靠

---

### 修复记录 - 2025-12-03

-   **问题描述：** Vercel 构建日志中仍然没有看到 `api/[...path].ts` 的信息，说明 Vercel 没有识别这个 Serverless Function 文件。即使将 `outputDirectory` 设置为 `"/"`，Vercel 仍然无法识别 `api` 文件夹中的函数。

-   **根本原因（可选）：** 
    1. Vercel 的 Serverless Functions 需要从 `outputDirectory` 指定的目录中查找
    2. 当 `outputDirectory` 设置为 `"/"` 时，Vercel 可能仍然无法正确识别项目根目录的 `api` 文件夹
    3. 将 `api` 文件夹复制到 `dist` 目录，让 Vercel 从构建输出目录中查找 Serverless Functions 更可靠

-   **解决方案：** 
    1. 创建 `build:api` 脚本，在构建后将 `api` 目录中的所有 `.ts` 文件复制到 `dist/api` 目录
    2. 更新 `build` 脚本，在 Vite 构建完成后执行 `build:api` 步骤
    3. 将 `vercel.json` 中的 `outputDirectory` 改回 `"dist"`，让 Vercel 从构建输出目录中查找 Serverless Functions
    4. `functions` 配置中的路径 `api/[...path].ts` 是相对于 `outputDirectory` 的，所以保持 `api/[...path].ts` 即可

-   **影响范围：**
    -   `package.json` - 添加 `build:api` 脚本，更新 `build` 脚本
    -   `vercel.json` - 将 `outputDirectory` 改回 `"dist"`

**功能特性：**
- ✅ `api` 文件夹会被复制到 `dist/api`，Vercel 可以从构建输出目录中识别 Serverless Functions
- ✅ 构建脚本自动处理 API 文件的复制
- ✅ 配置更加可靠，不依赖项目根目录的识别

**技术亮点：**
- 在构建过程中自动复制 `api` 文件夹到输出目录
- 使用 `readdirSync` 遍历并复制所有 `.ts` 文件，支持未来添加更多 API 文件
- 确保 Vercel 可以从构建输出目录中正确识别 Serverless Functions

---

### 修复记录 - 2025-12-03

-   **问题描述：** Node.js 运行时出现弃用警告：`[DEP0169] DeprecationWarning: url.parse() behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead.`

-   **根本原因（可选）：** 
    1. 警告来自依赖包（如 `@vercel/node` 或 `concurrently`），而不是项目代码本身
    2. 项目代码已经使用了 WHATWG URL API (`new URL()`)，没有直接使用 `url.parse()`
    3. 这是 Node.js 的弃用警告，不会影响功能，但会在日志中显示

-   **解决方案：** 
    1. 检查代码确认没有直接使用 `url.parse()`
    2. 项目代码已经正确使用了 WHATWG URL API (`new URL()`)
    3. 警告来自依赖包，可以通过以下方式处理：
       - 更新相关依赖包到最新版本（如果可用）
       - 或者忽略警告（不影响功能）
       - 或者在 Node.js 启动时使用 `--no-deprecation` 标志（不推荐，会隐藏所有弃用警告）

-   **影响范围：**
    -   无代码修改，仅文档说明

**功能特性：**
- ✅ 代码已使用 WHATWG URL API，符合最佳实践
- ✅ 警告不影响功能，只是信息提示

**技术亮点：**
- 使用 WHATWG URL API (`new URL()`) 替代已弃用的 `url.parse()`
- 代码符合 Node.js 最新标准

---

